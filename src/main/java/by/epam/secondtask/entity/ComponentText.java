package by.epam.secondtask.entity;

public interface ComponentText {
    void add(ComponentText componentText);
    void remove(ComponentText componentText);
    void read(ComponentText componentText);
}
